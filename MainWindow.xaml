<Window x:Class="ScanFolder.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:ScanFolder"
        xmlns:lvc="clr-namespace:LiveCharts.Wpf;assembly=LiveCharts.Wpf"
        mc:Ignorable="d"
        Title="Folder Scan" Height="450" Width="930" ResizeMode="CanMinimize" Icon="/folicon.png">
    <Window.Resources>
        <Style x:Key="FocusVisual">
            <Setter Property="Control.Template">
                <Setter.Value>
                    <ControlTemplate>
                        <Rectangle Margin="2" StrokeDashArray="1 2" Stroke="{DynamicResource {x:Static SystemColors.ControlTextBrushKey}}" SnapsToDevicePixels="true" StrokeThickness="1"/>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        <SolidColorBrush x:Key="Button.Static.Background" Color="#FFDDDDDD"/>
        <SolidColorBrush x:Key="Button.Static.Border" Color="#FF707070"/>
        <SolidColorBrush x:Key="Button.MouseOver.Background" Color="#FFBEE6FD"/>
        <SolidColorBrush x:Key="Button.MouseOver.Border" Color="#FF3C7FB1"/>
        <SolidColorBrush x:Key="Button.Pressed.Background" Color="#FFC4E5F6"/>
        <SolidColorBrush x:Key="Button.Pressed.Border" Color="#FF2C628B"/>
        <SolidColorBrush x:Key="Button.Disabled.Background" Color="#FFF4F4F4"/>
        <SolidColorBrush x:Key="Button.Disabled.Border" Color="#FFADB2B5"/>
        <SolidColorBrush x:Key="Button.Disabled.Foreground" Color="#FF838383"/>
        <Style x:Key="FolderButton" TargetType="{x:Type Button}">
            <Setter Property="FocusVisualStyle" Value="{StaticResource FocusVisual}"/>
            <Setter Property="Background" Value="{StaticResource Button.Static.Background}"/>
            <Setter Property="BorderBrush" Value="{StaticResource Button.Static.Border}"/>
            <Setter Property="Foreground" Value="{DynamicResource {x:Static SystemColors.ControlTextBrushKey}}"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="HorizontalContentAlignment" Value="Center"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
            <Setter Property="Padding" Value="1"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="{x:Type Button}">
                        <Border x:Name="border" Background="{TemplateBinding Background}" BorderBrush="{TemplateBinding BorderBrush}" BorderThickness="{TemplateBinding BorderThickness}" SnapsToDevicePixels="true">
                            <ContentPresenter x:Name="contentPresenter" Focusable="False" HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}" Margin="{TemplateBinding Padding}" RecognizesAccessKey="True" SnapsToDevicePixels="{TemplateBinding SnapsToDevicePixels}" VerticalAlignment="{TemplateBinding VerticalContentAlignment}"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsDefaulted" Value="true">
                                <Setter Property="BorderBrush" TargetName="border" Value="{DynamicResource {x:Static SystemColors.HighlightBrushKey}}"/>
                            </Trigger>


                            <Trigger Property="IsEnabled" Value="false">
                                <Setter Property="Background" TargetName="border" Value="{StaticResource Button.Disabled.Background}"/>
                                <Setter Property="BorderBrush" TargetName="border" Value="{StaticResource Button.Disabled.Border}"/>
                                <Setter Property="TextElement.Foreground" TargetName="contentPresenter" Value="{StaticResource Button.Disabled.Foreground}"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>
    <Grid>

        <ListBox Name="FileNamesListBox" Margin="422,10,181,10"/>
        <ListBox x:Name="FileExtensionListBox" SelectionChanged="FilterFiles" Margin="10,269,513,10"/>
        <lvc:PieChart Name="MyPieChart" Margin="71,57,581,187"/>
        <Canvas Margin="0,0,418,382">
            <Label Name="FolderNameLabel" FontSize="15px" Content="Choose Folder" FontWeight="Bold"  HorizontalContentAlignment="Left" Canvas.Left="53"/>
            <Button Click="FolderButtonClick" Style="{DynamicResource FolderButton}" Height="35" Width="34" Canvas.Left="19" BorderThickness="0" HorizontalAlignment="Left" VerticalAlignment="Top" Cursor="Hand" >
                <Button.Background>
                    <ImageBrush ImageSource="/folder.png"/>
                </Button.Background>
            </Button>
            <Label Name="CreatedDataLabel" Content="" Height="41" Canvas.Left="53" Canvas.Top="17" Width="364" HorizontalAlignment="Left" VerticalAlignment="Center"/>
        </Canvas>

        <Label Content="Name / Quantity" Margin="10,228,629,137" FontWeight="Bold" Panel.ZIndex="-1"/>
        <Label Name="FolderSizeLabel" Content="" Margin="324,229,366,136" FontWeight="Bold" Panel.ZIndex="-1"/>
        <Button Click="OpenFileClick" Content="Open File" HorizontalAlignment="Left" Height="26" Margin="754,10,0,0" VerticalAlignment="Top" Width="132"/>
        <Button Click ="DeleteFileClick" Content="Delete File" HorizontalAlignment="Left" Height="26" Margin="754,41,0,0" VerticalAlignment="Top" Width="132"/>
        <Button Click="OpenFileLocationClick" Content="Open file location" HorizontalAlignment="Left" Height="26" Margin="754,72,0,0" VerticalAlignment="Top" Width="132"/>
        <Button Click="OpenFilePropertiesClick" Content="File properties" HorizontalAlignment="Left" Height="26" Margin="754,103,0,0" VerticalAlignment="Top" Width="132"/>
        <Button Click="RefreshClick" Content="Refresh" HorizontalAlignment="Left" Height="26" Margin="754,134,0,0" VerticalAlignment="Top" Width="132"/>

    </Grid>
</Window>
